// Placeholder. T06 fills this package with the platform-agnostic domain
// (bako CRUD, membership rules, quota math) defined against ports.
// Dependency law: this package never imports Hono, @cloudflare/workers-types,
// or React Native (PRD §6).
export const CORE_PACKAGE = "@omoide/core";
